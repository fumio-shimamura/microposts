<h3>followers (<%= @user.follower_users.count %>)</h3>
  <ol class="microposts">
  <% @user.follower_users.each do |follower_user| %>
    <li>
    <span class="user"><%= gravatar_for follower_user %></span></br>
    <span class="user"><%= link_to follower_user.name , user_path(follower_user) %></span></br></br>
    </li>
    </br>
  <% end %>
</ol>
